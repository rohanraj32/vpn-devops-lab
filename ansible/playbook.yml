---
- name: Automate OpenVPN & Infrastructure Deployment
  hosts: localhost
  connection: local
  tasks:
    - name: Ensure configuration directories exist
      file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - ../openvpn
        - ../dashboard
        - ../monitoring